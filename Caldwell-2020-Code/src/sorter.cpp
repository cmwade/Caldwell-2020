#include "vex.h"

task ballsortertask = task(ballSort);

bool reject = false;
vision::signature unwantedColor;

void getBall() { //gets whether there is a ball in front of the sensor, and what color
  //TODO: write this
}

int ballSort() { //callback for the ball sorting task
  //TODO: write this
  return 0;
}